package lesson10.regform.service;
import org.springframework.stereotype.Service;
import lesson10.regform.dto.NoteDTO;

@Service
public class RegFormService {
    public String inputNote(NoteDTO name) {
        return "";
    }
}
